<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Configurações - PampaBooks</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body, html {
            height: 100%;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
        }

        .d-flex {
            height: 100%;
        }

        .sidebar {
            min-height: 100vh;
            background-color: #343a40;
            padding-top: 20px;
        }

        .sidebar a {
            color: white;
            padding: 15px;
            text-decoration: none;
            display: block;
        }

        .sidebar a:hover {
            background-color: #495057;
        }

        .main-content {
            flex-grow: 1;
            display: flex;
            flex-direction: column;
            padding: 20px;
        }

        .card {
            flex-grow: 1; /* Faz o card crescer para ocupar o espaço disponível */
            display: flex;
            flex-direction: column;
        }

        .card-body {
            flex-grow: 1;
            overflow-y: auto; /* Permite rolagem se o conteúdo for muito grande */
        }

        .card-header {
            background-color: #007bff;
            color: white;
        }

        .btn-logout {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="d-flex">
        <!-- Barra Lateral (Sidebar) -->
        <div class="sidebar bg-dark">
            <h4 class="text-center text-white mb-4">Menu</h4>
            <a href="/painel">Histórico de Compras</a>
            <a href="/painel/perfil">Perfil</a>
            <a href="/painel/config">Configurações</a>
            <a href="#" id="logoutLink" class="btn btn-danger btn-block mx-3 mt-4">Sair</a>
        </div>

        <!-- Conteúdo Principal -->
        <div class="main-content">
            <div class="container-fluid">
                <div class="text-center">
                    <h2>Configurações da Conta</h2>
                </div>

                <!-- Configurações -->
                <div class="card mt-4">
                    <div class="card-header">
                        <h4 class="mb-0">Preferências</h4>
                    </div>
                    <div class="card-body">
                        <p>Configurações da conta e preferências do usuário.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/javascripts/main.js"></script>
</body>
</html>
